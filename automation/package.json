{
  "name": "asyncdb-automation",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "End to end tests driving the UI in a browser",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "license": "AGPL-3.0",
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
